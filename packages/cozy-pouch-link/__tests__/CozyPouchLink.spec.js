import CozyPouchLink from '..'

describe('CozyPouchLink', () => {
    it('should check if the doctype is suported and forward if not')
    it('should check if the pouch is synced and forward if not')
    it('should be able to execute a query')
    it('should be able to execute a mutation query')
})
